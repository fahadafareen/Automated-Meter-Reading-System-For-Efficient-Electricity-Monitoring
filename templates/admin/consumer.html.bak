{% load static %}
{% block content %}

{% include 'admin/menu.html' %}

        <!-- partial -->
        <div class="main-panel">
          <div class="content-wrapper">
            <div class="row purchace-popup">
              <div class="col-12 stretch-card grid-margin">
               
              </div>
            </div>
           <center> <div class="col-md-6 grid-margin stretch-card">
                <div class="card">
                  <div class="card-body">
                    <h4 class="card-title">ADD CONSUMER</h4>
                    <form action="conss" method="post">
					{% csrf_token %}
<table>
<tr><td>
NAME:</td><td><select  name="name" class="form-control">
{% for i in result %}
<option value="{{i.name}}">{{i.name}}</option>
{% endfor %}
</select>
<br><br>
</td></tr>

<tr><td>
METER-NUMBER:</td><td><select name="meternumber" class="form-control">
{% for i in result1 %}
<option value="{{i.meternumber}}">{{i.meternumber}}</option>
{% endfor %}


</select><br><br>
</td></tr>
<tr><td>
<input type="submit" name="SUBMIT">
</td></tr>
</table>
</form>
                  </div>
                </div>
              </div></center>
     
                  </div>
                </div>
              </div>
            </div>
          </div>
          <!-- content-wrapper ends -->
          <!-- partial:partials/_footer.html -->
          
        <!-- main-panel ends -->
      </div>
      <!-- page-body-wrapper ends -->
    </div>
    <!-- container-scroller -->
    <!-- plugins:js -->
 {% include 'admin/footer.html' %}

    {% endblock %}